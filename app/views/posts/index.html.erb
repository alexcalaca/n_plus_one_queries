<% @posts.each do |post| %>
  <h2><%= post.title %></h2>
  <ul>
    <% puts "#" * 5 + " comments for post #{post.id}" %>
    <% post.comments.each do |comment| %>
      <li><%= comment.body %></li>
    <% end %>
  </ul>
<% end %>